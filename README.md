# OpenShift Deployment Examples – YAML, Kustomize, Helm

This repo contains three ways to deploy the same simple app (**hello-openshift**) to OpenShift:

- `plain-manifests/` – raw Kubernetes/OpenShift YAML
- `kustomize/` – base + overlays
- `helm/` – a simple Helm chart

Follow instructions inside each folder.
